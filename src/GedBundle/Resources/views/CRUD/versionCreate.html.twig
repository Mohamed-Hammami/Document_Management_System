
{% block version_create %}
    <form action="{{ path('version_create', {'id': id}) }}" method="post"{{form_enctype(form) }}>
        <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal">&times;<span class="sr-only">Close</span></button>
            <h4 class="modal-title" id="myModalLabel">Create New Version</h4>
        </div>
        <div class="modal-body">
            <div class="form-group">
                {{ form_label(form.type) }}
                {{ form_errors(form.type) }}
                {{ form_widget(form.type) }}
            </div>
            <div class="form-group">
                {{ form_label(form.fileContent) }}
                {{ form_errors(form.fileContent) }}
                {{ form_widget(form.fileContent) }}
            </div>
        </div>
        <div class="modal-footer">
            <div class="pull-left">
                {{ form_widget(form.create) }}
            </div>
            <div class="pull-right">
                <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
            </div>
        </div>
        {{ form_rest(form) }}
    </form>
{% endblock %}
